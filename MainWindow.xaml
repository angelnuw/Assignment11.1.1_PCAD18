<Window x:Class="Assignment11._1._1.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        mc:Ignorable="d"
        Title="Books Inventory" Height="520" Width="900">
    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Title -->
        <TextBlock Text="Books Inventory (WPF + EF Core Code-First)"
                   FontSize="18" FontWeight="Bold" Margin="0,0,0,10"/>

        <!-- Input panel -->
        <StackPanel Grid.Row="1" Orientation="Horizontal">
            <StackPanel Width="200" Margin="0,0,10,0">
                <TextBlock Text="ISBN"/>
                <TextBox x:Name="txtISBN"/>
            </StackPanel>

            <StackPanel Width="220" Margin="0,0,10,0">
                <TextBlock Text="Name"/>
                <TextBox x:Name="txtName"/>
            </StackPanel>

            <StackPanel Width="200" Margin="0,0,10,0">
                <TextBlock Text="Author"/>
                <TextBox x:Name="txtAuthor"/>
            </StackPanel>

            <StackPanel Width="240" Margin="0,0,10,0">
                <TextBlock Text="Description"/>
                <TextBox x:Name="txtDescription"/>
            </StackPanel>

            <StackPanel VerticalAlignment="Bottom" Orientation="Horizontal">
                <Button x:Name="btnAdd"     Content="Add"              Click="btnAdd_Click"     Width="70"  Margin="0,0,8,0"/>
                <Button x:Name="btnDelete"  Content="Delete Selected"  Click="btnDelete_Click" Width="120" Margin="0,0,8,0"/>
                <Button x:Name="btnSave"    Content="Save Changes"     Click="btnSave_Click"    Width="110" Margin="0,0,8,0"/>
                <Button x:Name="btnRefresh" Content="Refresh"          Click="btnRefresh_Click" Width="80"/>
            </StackPanel>
        </StackPanel>

        <!-- Data grid -->
        <DataGrid x:Name="BooksGrid" Grid.Row="2" Margin="0,10,0,0"
                  AutoGenerateColumns="False"
                  CanUserAddRows="False" IsReadOnly="False"
                  SelectionMode="Single" SelectionUnit="FullRow">
            <DataGrid.Columns>
                <DataGridTextColumn Header="ISBN"        Binding="{Binding ISBN}"        Width="150"/>
                <DataGridTextColumn Header="Name"        Binding="{Binding Name}"        Width="250"/>
                <DataGridTextColumn Header="Author"      Binding="{Binding Author}"      Width="200"/>
                <DataGridTextColumn Header="Description" Binding="{Binding Description}" Width="*"/>
            </DataGrid.Columns>
        </DataGrid>
    </Grid>
</Window>

